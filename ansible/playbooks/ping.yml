---
- name: Refresh SSH known_hosts entries
  import_playbook: refresh_known_hosts.yml

- name: Confirm Terraform-managed hosts respond
  hosts: target_hosts
  gather_facts: false
  tasks:
    - name: Ping guests across both VM and container groups
      ansible.builtin.ping: {}
