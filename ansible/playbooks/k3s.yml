---
# Placeholder playbook for k3s. Implement role or tasks as needed.
- name: k3s - placeholder
  hosts: k3s
  become: true
  vars: {}
  tasks:
    - name: Placeholder - implement k3s role or tasks
      ansible.builtin.debug:
        msg: "Add k3s role/playbook and update this file"
