apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mailu
  namespace: mailu
spec:
  rules:
  - http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: mailu-admin
            port:
              number: 8080
