repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.24.2
    hooks:
      - id: gitleaks
        stages: [pre-commit, pre-push]
        args:
          - --redact
          - --config=.gitleaks.toml
