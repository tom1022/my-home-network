apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres-cluster-backup
  namespace: postgres
spec:
  immediate: true
  # CNPG parses this field as 6-term robfig/cron (leading seconds), not 5-term crontab.
  schedule: "0 0 0,12 * * *"
  cluster:
    name: postgres-cluster
